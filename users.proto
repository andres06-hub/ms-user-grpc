syntax = "proto3";

package users;
option go_package="./users";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Users {
  rpc Ping(Request) returns(Response);
}
